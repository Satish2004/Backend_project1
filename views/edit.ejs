<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Edit Message</title>
  <link rel="stylesheet" href="style.css" />
  <style>
    /* Global styles */
    * {
      margin: 0;
      padding: 0;
      box-sizing: border-box;
    }

    html,
    body {
      height: 100%;
      width: 100%;
      font-family: Arial, sans-serif;
      background-color: #3772ca;
    }

    /* Center the form container */
    .form-container {
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100%;
    }

    .message-form {
      background-color: #ffffff;
      border-radius: 8px;
      box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
      padding: 20px;
      width: 100%;
      max-width: 400px;
    }

    .message-form label {
      display: block;
      font-size: 16px;
      margin-bottom: 10px;
      color: #333;
    }

    .message-form textarea {
      width: 100%;
      height: 100px;
      padding: 10px;
      border: 1px solid #ccc;
      border-radius: 4px;
      resize: none;
      font-size: 14px;
      color: #333;
    }

    .message-form textarea:focus {
      border-color: #007bff;
      outline: none;
    }

    .edit-button {
      background-color: #007bff;
      color: #ffffff;
      border: none;
      border-radius: 4px;
      padding: 10px 20px;
      font-size: 16px;
      cursor: pointer;
      transition: background-color 0.3s;
      width: 100%;
      margin-top: 10px;
    }

    .edit-button:hover {
      background-color: #0056b3;
    }

    .edit-button:focus {
      outline: none;
    }

    .headerChat {
      color: #333;
      left: 20%;
      margin: 10px;
      border: 1px solid white;
      background-color: rgb(199, 171, 11);
      border-radius: 20px;
      padding: 10px;
    }
  </style>
</head>
<body>
  <div class="headerChat">
    <h1>Edit Chat</h1>
    <p>id = <%= chat._id %></p>
    <span>This chat was sent from <%= chat.from %> to <%= chat.to %></span>
  </div>

  <hr />

  <div class="form-container">
    <form class="message-form" action="/chats/<%= chat._id %>?_method=PUT" method="POST">
      <label for="message">Message:</label>
      <textarea id="message" name="message" placeholder="Type your message here..."><%= chat.msg %></textarea>
      <button type="submit" class="edit-button">Edit</button>
    </form>
  </div>
</body>
</html>
